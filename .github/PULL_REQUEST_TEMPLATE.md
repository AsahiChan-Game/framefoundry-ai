## 变更说明

说明这次改动解决的问题和用户可见结果。

## 验证

- [ ] `npm test`
- [ ] `python -m unittest discover -s backend/tests -v`
- [ ] 未提交 `.env`、运行数据、素材、模型或本机绝对路径

## 真实生成影响

如涉及 ComfyUI、模型节点或 GPU 调度，请说明实际验证的工作流、节点状态和未验证边界。
