# 参与贡献

感谢你帮助改进帧造工场。

## 开发流程

1. 从新分支开始，保持改动聚焦。
2. 不提交模型权重、生成素材、个人路径或密钥。
3. 新功能应保留“真实状态”和“模拟状态”的可辨识性。
4. 修改任务调度时，优先保证单 GPU 环境不会并发抢占显存。
5. 提交前运行：

```powershell
npm test
.\.venv\Scripts\python.exe -m unittest discover -s backend/tests -v
```

## Pull Request 说明

请写明：

- 改了什么以及为什么；
- 实际运行了哪些验证；
- 是否依赖特定 ComfyUI 自定义节点；
- 界面显示的是实际数据、模拟数据还是尚未接通的占位状态。

## 工作流样例

不要提交未经验证或依赖不明的节点图。样例需要说明来源、依赖的自定义节点、模型文件名、
显存需求和已验证的 ComfyUI 版本。
