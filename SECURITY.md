# 安全说明

## 支持范围

当前支持安全修复的版本为 `1.0.x`。

## 本地边界

- 控制 API 默认只监听 `127.0.0.1:8766`，请不要直接暴露到公网。
- `.env`、SQLite 数据库、上传素材、生成产物和日志不应提交到 Git。
- ComfyUI 工作流可能调用第三方自定义节点；只加载你信任并检查过的 JSON 与节点代码。
- 节点地址来自本机配置，不允许普通 API 请求指定任意远程 URL，以降低 SSRF 风险。
- 上传文件有扩展名允许列表和默认 20 MiB 上限，但这不能替代恶意文件扫描。

## 报告问题

项目发布到 GitHub 后，请优先使用仓库的 Private vulnerability reporting / Security Advisory，
不要在公开 Issue 中粘贴密钥、个人路径、素材内容或可直接利用的细节。
